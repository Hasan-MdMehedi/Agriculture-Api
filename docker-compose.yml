version: "3.9"

services:
  api:
    build: .
    ports:
      - "5000:8000"
    environment:
      HOST: ${HOST}
      PORT: ${PORT:-3306}
      USER: ${USER}
      PASSWORD: ${PASSWORD}
      DB: ${DB:-agriculture_db}
    env_file:
      - .env
    restart: unless-stopped
